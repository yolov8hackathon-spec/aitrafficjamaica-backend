{
  "name": "whitelinez-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
